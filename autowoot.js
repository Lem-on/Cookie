API.chatLog("Cookie Loaded"); 
API.on(API.ADVANCE, aw); /* autowoot */
API.on(API.ADVANCE, a); function a(b){ if(b.message == "ahoj"){ API.sendchat("ahoj :)";)}}

/* autowoot */
function aw(){ 
	$("#woot").click(); 
}

